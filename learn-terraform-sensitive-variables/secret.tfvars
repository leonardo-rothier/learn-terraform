# This is just a test environment, keep in mind that you NEVER commit your variables files as tfvars to your version code repo (as with our terraform.tfstate)

db_username = "admin"
db_password = "insecurepassword"